# Benchmark Results

This benchmark compares a standard LLM conversation (No-memory) with our LangGraph Multi-Memory Agent (With-memory).

| # | Scenario | No-memory result | With-memory result | Pass? |
|---|----------|------------------|---------------------|-------|
| 1 | Profile recall (Name) | Hiện tại, tôi không có thông tin về tên của bạn. Nếu bạn muốn, bạn có thể cho tôi biết tên của bạn để tôi có thể ghi nhớ! | Có chứ, tên bạn là Linh. Nếu bạn cần giúp đỡ gì, hãy cho mình biết nhé! | Pass |
| 2 | Conflict update (Allergy) | Tôi không có thông tin về tình trạng sức khỏe của bạn, vì vậy không thể xác định bạn có bị dị ứng sữa bò hay không. Nếu bạn nghi ngờ mình có dị ứng, tốt nhất là nên tham khảo ý kiến bác sĩ để được kiểm tra và tư vấn chính xác. | Bạn đã thông báo rằng bạn bị dị ứng đậu nành, nhưng không có thông tin nào cho thấy bạn bị dị ứng sữa bò. Nếu bạn cần thêm thông tin hoặc có câu hỏi khác, hãy cho mình biết nhé! | Pass |
| 3 | Conflict update (Allergy recall) | Hiện tại, tôi không có thông tin về dị ứng của bạn. Nếu bạn có thể chia sẻ thêm, tôi sẽ giúp bạn tốt hơn! | Bạn bị dị ứng với đậu nành. Nếu bạn cần thêm thông tin hoặc có câu hỏi khác, hãy cho mình biết nhé! | Pass |
| 4 | Episodic recall (Task completion) | Hiện tại, tôi không có thông tin về các module bạn đã hoàn thành. Nếu bạn có thể cung cấp thêm chi tiết hoặc nhắc đến module cụ thể nào, tôi sẽ cố gắng hỗ trợ bạn tốt hơn! | Bạn vừa hoàn thành module trích xuất dữ liệu. Nếu bạn cần hỗ trợ gì thêm về module này hoặc có câu hỏi khác, hãy cho mình biết nhé! | Pass |
| 5 | Semantic retrieval (FAQ chunk 1) | LangGraph là một công cụ hoặc nền tảng thường được sử dụng để xây dựng và phân tích các mô hình ngôn ngữ. Nó có thể hỗ trợ trong việc phát triển các ứng dụng liên quan đến xử lý ngôn ngữ tự nhiên (NLP), như tạo ra chatbot, phân tích cảm xúc, hoặc dịch thuật. LangGraph giúp người dùng dễ dàng hình dung và tối ưu hóa các mối quan hệ giữa các từ và cụm từ trong ngôn ngữ, từ đó cải thiện hiệu suất của các mô hình ngôn ngữ. | LangGraph là một công cụ hoặc thư viện thường được sử dụng trong lĩnh vực xử lý ngôn ngữ tự nhiên (NLP) và học máy. Nó giúp xây dựng và quản lý các mô hình ngôn ngữ, cho phép người dùng dễ dàng tạo ra các biểu đồ ngôn ngữ, phân tích cú pháp, hoặc thực hiện các tác vụ liên quan đến ngôn ngữ khác. Nếu bạn cần thông tin chi tiết hơn về cách sử dụng hoặc tính năng cụ thể của LangGraph, hãy cho mình biết nhé! | Fail |
| 6 | Semantic retrieval (FAQ chunk 2) | Port mặc định của PostgreSQL là 5432. | Port mặc định của PostgreSQL là 5432. Nếu bạn cần thêm thông tin hoặc có câu hỏi khác, hãy cho mình biết nhé! | Pass |
| 7 | Semantic retrieval (Docker config) | Để kết nối tới database ở một container khác, bạn có thể làm theo các bước sau:  1. **Kiểm tra mạng**: Đảm bảo rằng cả hai container (container chứa ứng dụng và container chứa database) đều nằm trong cùng một mạng Docker. Bạn có thể tạo một mạng mới bằng lệnh:    ```bash    docker network create my_network    ```  2. **Khởi động container database**: Khi khởi động container database, hãy chỉ định mạng mà bạn đã tạo:    ```bash    docker run --name my_database --network my_network -e MYSQL_ROOT_PASSWORD=my-secret-pw -d mysql    ```  3. **Khởi động container ứng dụng**: Tương tự, khi khởi động container ứng dụng, hãy chỉ định cùng một mạng:    ```bash    docker run --name my_app --network my_network -d my_app_image    ```  4. **Kết nối tới database**: Trong ứng dụng của bạn, sử dụng tên container của database (ví dụ: `my_database`) làm hostname để kết nối. Ví dụ, nếu bạn sử dụng MySQL, chuỗi kết nối có thể trông như sau:    ```plaintext    mysql -h my_database -u root -p    ```  5. **Kiểm tra firewall**: Nếu bạn gặp vấn đề kết nối, hãy kiểm tra các cài đặt firewall hoặc cấu hình bảo mật khác có thể ngăn chặn kết nối giữa các container.  Nếu bạn cần thêm thông tin chi tiết về một loại database cụ thể, hãy cho tôi biết! | Để kết nối tới database ở một container khác, bạn có thể làm theo các bước sau:  1. **Kiểm tra mạng**: Đảm bảo rằng cả hai container (container chứa ứng dụng và container chứa database) đều nằm trong cùng một mạng Docker. Bạn có thể tạo một mạng mới bằng lệnh:    ```bash    docker network create my_network    ```  2. **Khởi động container database**: Khi khởi động container database, hãy chỉ định mạng mà bạn đã tạo:    ```bash    docker run --name my_database --network my_network -e POSTGRES_PASSWORD=mysecretpassword -d postgres    ```  3. **Khởi động container ứng dụng**: Tương tự, khi khởi động container ứng dụng, hãy chỉ định cùng một mạng:    ```bash    docker run --name my_app --network my_network -d my_app_image    ```  4. **Kết nối tới database**: Trong ứng dụng của bạn, sử dụng tên container của database (trong ví dụ này là `my_database`) làm hostname để kết nối. Ví dụ, nếu bạn sử dụng PostgreSQL, chuỗi kết nối có thể trông như sau:    ```    postgres://username:password@my_database:5432/mydatabase    ```  5. **Kiểm tra kết nối**: Đảm bảo rằng bạn đã cấu hình đúng thông tin đăng nhập và tên database trong ứng dụng của bạn.  Nếu bạn cần thêm thông tin chi tiết hoặc gặp vấn đề cụ thể, hãy cho mình biết nhé! | Pass |
| 8 | Trim/token budget (Long conversation) | Số đầu tiên chúng ta đếm thường là số 1. | Số đầu tiên chúng ta đếm là 1. | Pass |
| 9 | Profile recall (Preferences) | Hiện tại, tôi không có thông tin về sở thích màu sắc của bạn. Nếu bạn muốn chia sẻ, tôi có thể giúp bạn tìm hiểu thêm về màu sắc đó hoặc gợi ý các lựa chọn phù hợp! | Bạn thích giao diện dark mode (nền tối). Nếu bạn có thêm sở thích nào khác về màu sắc, hãy cho tôi biết nhé! | Pass |
| 10 | Episodic recall (Past decision) | Hiện tại, tôi không có thông tin về các cuộc trò chuyện trước đây của chúng ta. Bạn có thể cho tôi biết thêm chi tiết về quyết định đó không? | Hôm qua, chúng ta đã chốt dùng Redis để cache. Nếu bạn cần thêm thông tin hoặc hỗ trợ về Redis, hãy cho tôi biết nhé! | Pass |
